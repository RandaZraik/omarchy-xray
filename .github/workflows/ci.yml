name: CI

on:
  push:
    branches: [master]
  pull_request:
  workflow_call:
    inputs:
      target:
        description: Exact commit to validate when called by another workflow
        required: false
        type: string

permissions:
  contents: read

jobs:
  portable-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out the reviewed commit
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ inputs.target || github.sha }}
          persist-credentials: false

      - name: Compile the backend
        run: python3 -m compileall -q backend

      - name: Install the portable Qt runtime
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends python3-pytest qml-qt6 qml6-module-qtquick qt6-declarative-dev-tools

      - name: Run the complete portable test tier
        run: python3 -m pytest -m portable

      - name: Check portable QML and JavaScript syntax
        run: |
          QMLFORMAT="$(command -v qmlformat || true)"
          if [ -z "$QMLFORMAT" ]; then
            QMLFORMAT=/usr/lib/qt6/bin/qmlformat
          fi
          QMLLINT="$(command -v qmllint || true)"
          if [ -z "$QMLLINT" ]; then
            QMLLINT=/usr/lib/qt6/bin/qmllint
          fi
          while IFS= read -r -d '' file; do
            "$QMLFORMAT" "$file" >/dev/null
          done < <(find . -type f -name '*.qml' -not -path './.git/*' -print0)
          find . -type f -name '*.js' -not -path './.git/*' \
            -exec "$QMLLINT" {} +
